describe('UserResolver', () => {
  describe('signUp', () => {
    it('throws an error if the email is already in use', () => {});

    it('throws an error if the username is already in use', () => {});

    it('throws an error if the passwords do not match', () => {});

    it('returns the user data and a jwt token given a valid sign up input', () => {});
  });
});
