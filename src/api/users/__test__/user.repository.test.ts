describe('UserRepository', () => {
  describe('signUp', () => {
    it('throws an error if the email is already in use', async () => {});

    it('throws an error if the username is already is use', async () => {});

    it('throws an error if the passwords do not match', async () => {});

    it('successfully creates a user given a valid input', async () => {});
  });
});
